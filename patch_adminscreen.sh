sed -i 's/                \/\/ EMPLOYEES OF THIS COMPANY SECTION/                \n                var showRoleDialog by remember { mutableStateOf(false) }\n                if (showRoleDialog) {\n                    RoleManagementDialog(\n                        company = currentCompany,\n                        onSave = { updatedCompany -> \n                            val updatedList = companies.map { if (it.companyId == updatedCompany.companyId) updatedCompany else it }\n                            adminViewModel.saveCompany(updatedCompany)\n                            showRoleDialog = false\n                        },\n                        onDismiss = { showRoleDialog = false }\n                    )\n                }\n                Button(\n                    onClick = { showRoleDialog = true },\n                    modifier = Modifier.fillMaxWidth().padding(vertical = 8.dp),\n                    colors = ButtonDefaults.buttonColors(containerColor = NeonBlue)\n                ) { \n                    Icon(Icons.Default.ManageAccounts, contentDescription = null, tint = White)\n                    Spacer(modifier = Modifier.width(8.dp))\n                    Text("Quản lý Chức vụ", color = White)\n                }\n                \n                \/\/ EMPLOYEES OF THIS COMPANY SECTION/g' app/src/main/java/com/example/ui/screens/AdminScreen.kt
